
# Install scimap first with its dependencies
scimap>=2.0.0

# Install tercen client last
# Note: Dockerfile installs this with --no-deps to avoid version conflicts
git+https://github.com/tercen/tercen_python_client@0.12.12
